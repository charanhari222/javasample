version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto8.x
  build:
    commands:
      - mvn clean package
artifacts:
   files:
      - '**/*'
      - appspec.yml
   discard-paths: yes
